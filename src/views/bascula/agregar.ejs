<%- include('../partials/head') %>

<div class="container mt-5">
    <h1 class="mb-4">Agregar Registro de Báscula</h1>
    
    <div class="row">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title mb-3">Paciente: <%= paciente.nombre %> <%= paciente.apellidos %></h5>
                    
                    <form action="/bascula/<%= paciente.id %>" method="POST">
                        <div class="mb-3">
                            <label for="peso" class="form-label">Peso (kg):</label>
                            <input type="number" step="0.01" class="form-control" id="peso" name="peso" required>
                        </div>
                        
                        <div class="mb-3">
                            <label for="altura" class="form-label">Altura (m):</label>
                            <input type="number" step="0.01" class="form-control" id="altura" name="altura" required>
                        </div>
                        
                        <div class="mb-3">
                            <label for="fecha" class="form-label">Fecha:</label>
                            <input type="date" class="form-control" id="fecha" name="fecha" required>
                        </div>
                        
                        <button type="submit" class="btn btn-primary">Guardar Registro</button>
                        <a href="/bascula/<%= paciente.id %>" class="btn btn-secondary">Cancelar</a>
                    </form>
                </div>
            </div>
        </div>
        
        <div class="col-md-4">
            <div class="card bg-light">
                <div class="card-body">
                    <h5 class="card-title">Información</h5>
                    <p class="card-text">
                        <small>Ingrese el peso en kilogramos y la altura en metros. El IMC se calculará automáticamente basado en estos valores.</small>
                    </p>
                    <h6 class="mt-3">Fórmula IMC</h6>
                    <p class="card-text"><small>IMC = Peso (kg) / Altura² (m)</small></p>
                </div>
            </div>
        </div>
    </div>
    
    <div class="mt-4">
        <a href="/bascula/<%= paciente.id %>" class="btn btn-secondary">Volver a Báscula</a>
    </div>
</div>

<%- include('../partials/footer') %>
